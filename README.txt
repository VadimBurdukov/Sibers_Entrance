1. Import dump on your server (I used the local one for testing);
2. Copy all the folder of archive to the root folder of project;
3. Change the data for connection to database in /app/config/Db.php;
